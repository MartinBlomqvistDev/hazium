"""Screens: transparent rules over public facts, where a model cannot be fitted.

A screen is not a model and this package exists to keep the two apart. HEWB
learns weights from 102 withdrawal events. The TFA screen has six externally
confirmed substances, which is enough to *check* a rule and nowhere near enough
to fit one, so its weights are stated in the source rather than estimated from
the data they would then be validated against.
"""
